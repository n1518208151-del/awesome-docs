# 模块示例

本章按从底层到应用的顺序，覆盖多媒体基础能力、AI 算法参考实现、高效开发中间件，以及按平台分类的完整示例。

```{toctree}
:maxdepth: 1

multimedia
ai_cv
ai_audio
ai_llm
middleware
samples_board
samples_axcl
```

**本章内容**

- **多媒体基础能力**：音频、显示输出、ISP、编解码。
- **AI 算法参考实现**：CV（检测/分割/人脸）、Audio（ASR/TTS/降噪）、大模型（LLM/VLM）。
- **高效开发中间件**：流媒体处理 (AX-Video, AX-Pipeline)、业务逻辑组件。
- **示例展示（按平台）**：主控开发板示例、算力卡平台（指向 AXCL 文档）。

```{note}
`samples_*` 页面为按硬件平台组织的现有示例汇总，后续将逐步按算法类别重组到对应页面。
```

